// Servicio de IA eliminado a petición del usuario.
// Este archivo se mantiene vacío para evitar errores de importación residuales si los hubiera.
export const generateTShirtDesign = async (prompt: string): Promise<string> => {
  throw new Error("AI Service is disabled");
};